ReaderPaths = ["/uptime", "/free", "/df", "/iostat", "/net/io", "/net/interfaces", "/cpu/info", "/uname", "/ResourcedMaster", "/ResourcedAgent"]
Path = "/resourced-master/host"
GoStruct = "ResourcedMasterHost"
Interval = "60s"

[GoStructFields]
Method = "POST"

# If you set AccessToken on general.toml,
# and if you don't set access token here,
# The access token on general.toml will be used.
# Username = "{access-token}"

MaxRetries = 3
